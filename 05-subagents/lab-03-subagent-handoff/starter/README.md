# starter

You build a two-subagent pipeline: `reviewer` (read-only) produces a findings report, `patch-writer` (Edit-capable) takes findings one at a time and applies fixes. Write both agents, document the dispatch flow in `PIPELINE.md`, then exercise the pipeline against `src/config.py`.

See the top-level lab README for the step-by-step.
