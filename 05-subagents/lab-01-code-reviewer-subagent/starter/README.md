# starter

You build a project-scoped `code-reviewer` subagent at `.claude/agents/code-reviewer.md` and dispatch it against `src/auth.py`, which has three seeded problems.

See the top-level lab README for the step-by-step.
