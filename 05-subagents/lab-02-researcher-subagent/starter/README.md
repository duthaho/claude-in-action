# starter

You build a read-only `researcher` subagent at `.claude/agents/researcher.md`. The `docs/` directory is the corpus it answers questions against.

See the top-level lab README for the step-by-step.
