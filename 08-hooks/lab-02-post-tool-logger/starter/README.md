# starter

You wire a `PostToolUse` hook that logs every `Edit` or `Write` call to `.claude/logs/edits.log`. The log file is gitignored; the hook script and the wiring are checked in.

See the top-level lab README for the step-by-step.
