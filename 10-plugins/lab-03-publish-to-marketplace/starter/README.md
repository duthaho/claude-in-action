# Starter — lab-03-publish-to-marketplace

The `marketplace/` directory ships two fully-built plugins (`quote-of-the-day` and `daily-standup`). Its `marketplace.json` is empty. You fill it in so both plugins are listed, then register the marketplace and confirm both plugins appear.

Open the lab's `README.md` and follow `## Steps`.
