# Starter — lab-02-author-a-plugin

The `quote-of-the-day/` directory is a plugin scaffold with empty placeholders. You fill in:

- `.claude-plugin/plugin.json` — the manifest is empty
- `commands/quote.md` — does not exist yet
- `skills/wordcount/SKILL.md` — does not exist yet

`quotes.txt` is pre-filled — don't edit it. Open the lab's `README.md` and follow `## Steps`.
