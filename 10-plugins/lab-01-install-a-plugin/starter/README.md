# Starter — lab-01-install-a-plugin

The `marketplace/` directory is a fully-built plugin marketplace. You do not edit it. You register it with Claude Code and install the `greeter` plugin it ships.

Open the parent `README.md` and follow `## Steps`.
