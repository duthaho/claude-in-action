# Starter — lab-02-imports-and-layers

The `CLAUDE.md` in this directory is intentionally bloated. Your job is to split it into three layers:

1. User-wide rules → `fake-home/.claude/CLAUDE.md` (create it).
2. Project rules → `CLAUDE.md` (edit it down).
3. API conventions → `docs/memory/api-style.md` (create it, reference it with `@import`).

See the lab README one level up for the full steps.
