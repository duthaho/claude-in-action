# Starter — lab-04-organizing-rules

`CLAUDE.md` is a monolithic memory file covering four distinct topics (architecture, testing, commit hygiene, style). Your job is to decompose it into topic files under `.claude/rules/` and trim `CLAUDE.md` down to a short summary.

See the lab README one level up for the full steps.
