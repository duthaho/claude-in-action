# Project Memory — todo-cli

## Architecture

- Entry point: `todo.py`. State in `./todos.json`.

## Workflow

- When asked to add a feature, you should avoid committing before the user approves.
- Propose the change first.
- Wait for explicit approval on the diff.

## Rules

- Standard library only.
- Do not touch code you were not asked to change.
