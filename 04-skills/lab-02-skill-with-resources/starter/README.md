# Starter — lab-02-skill-with-resources

The starter has a `docs/adr/` directory with one existing ADR (`0001-use-python.md`). Your job is to build an `adr-writer` skill under `.claude/skills/adr-writer/` that bundles two template files as resources and writes new ADRs into this directory.

See the lab README one level up for the full steps.
