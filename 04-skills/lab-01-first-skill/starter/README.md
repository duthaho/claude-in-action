# Starter — lab-01-first-skill

Run `bash bootstrap.sh` to turn this directory into a small git repo with a `v0.1.0` tag and five commits on top. Then author `.claude/skills/changelog-writer/SKILL.md` per the lab README.
