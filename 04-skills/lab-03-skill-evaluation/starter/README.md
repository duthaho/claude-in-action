# Starter — lab-03-skill-evaluation

Three things to know:

- `.claude/skills/slug-generator/SKILL.md` — the skill under test.
- `.claude/skills/slug-generator/evals.json` — three eval cases. Two have wrong expected values. Fix them, add a fourth real case, and add a fifth expected-failure case.
- `evals_runner.py` — the test harness. Run `python evals_runner.py` to see pass/fail.

See the lab README one level up for the full steps.
