# Starter — lab-03-per-project-vs-user

Two settings files are pre-populated and disagree. Your job is to predict which value wins for each key, write the prediction to `analysis.md`, then edit the project file so intent matches behavior.

See the lab README one level up for the full steps.
