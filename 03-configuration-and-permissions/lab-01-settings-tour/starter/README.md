# Starter — lab-01-settings-tour

The `.claude/` directory does not exist yet. Create it, then author `settings.json` and `settings.local.json` per the lab instructions one level up.

Don't forget to add `.claude/settings.local.json` to `.gitignore` in step 7.
