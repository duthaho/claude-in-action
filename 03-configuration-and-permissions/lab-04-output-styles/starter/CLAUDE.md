# Project — sample-lab

A tiny scratch project used in the output-styles lab. No source files — this directory exists so the lab has a place to boot Claude from.

## Conventions

- Python 3, standard library only.
- Run tests with `python -m unittest`.
- Double quotes for string literals.
