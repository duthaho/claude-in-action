# Starter — lab-03-custom-mcp-stub

Three things to know:

- `server.py` — scaffold with a `main()` loop. You fill in `handle()` and populate `QUOTES`.
- `test_server.py` — stdlib unittest that drives the server over stdin/stdout. Your implementation passes when all four tests pass.
- `.mcp.json` — not yet written. You create it in step 6.

See the lab README one level up for the full steps.
