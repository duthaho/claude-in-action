# Starter — lab-02-branching-explorations

`src/sorter.py` has an unimplemented `sort_events`. `tests/test_sorter.py` pins the expected behaviour — including a tie-breaking case that Branch A will *not* satisfy, on purpose, so the comparison is meaningful.

Do not edit `src/sorter.py` directly here before the lab — leaving it as a stub is what lets `/rewind` fork cleanly between branches. Open the lab's `README.md` and follow `## Steps`.
