# Starter — lab-01-rewind-a-mistake

`src/calculator.py` is a working `Calculator` class with `add` and `subtract`. You will ask Claude to add a broken `divide`, observe the file change, then rewind the session to return to this exact state.

Open the lab's `README.md` and follow `## Steps`.
