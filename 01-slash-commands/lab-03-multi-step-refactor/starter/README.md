# Starter — lab-03-multi-step-refactor

`messy.py` is intentionally broken: it has a typo (`nam` instead of `name`) and no trailing newline. `test_messy.py` verifies `greeting("world") == "hello, world"`.

Your job is to write `.claude/commands/refactor.md` and then run `/refactor messy.py` inside Claude Code. See the lab README one level up for the full steps.
