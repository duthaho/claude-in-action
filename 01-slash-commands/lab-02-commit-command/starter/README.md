# Starter — lab-02-commit-command

1. Run `bash bootstrap.sh` from this directory to initialize a git repo with staged changes.
2. Create `.claude/commands/commit.md` — see the lab README for the details.
3. Launch Claude Code from this directory and run `/commit`.
