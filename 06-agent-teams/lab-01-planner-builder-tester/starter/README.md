# starter

You enable agent teams via `.claude/settings.json`, define three teammate roles (`planner`, `builder`, `tester`) as subagents in `.claude/agents/`, write `SPEC.md` describing the feature, and author `TEAM-PROMPT.md` — the exact prompt the learner pastes to the lead.

See the top-level lab README for the step-by-step.
