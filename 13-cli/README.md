# Section 13 — CLI

> Status: **coming in v2.0**

Running Claude Code from scripts and CI, headless. Planned labs:

- `lab-01-cli-flags-tour` — drive Claude entirely from CLI flags, no TUI.
- `lab-02-scripting-with-claude` — a bash pipeline that feeds a log file through `claude -p` and post-processes the result.
- `lab-03-ci-integration` — a GitHub Actions workflow that runs Claude on each pull request.

## References

- [Official docs: CLI reference](https://docs.claude.com/en/docs/claude-code/cli-reference)
